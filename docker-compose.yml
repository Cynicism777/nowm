services:
  nowm:
    build: .
    image: nowm:latest
    container_name: nowm
    ports:
      - "8823:8000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
